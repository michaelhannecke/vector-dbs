**/.DS_Store
**/.venv
**/.env


# terraform----
# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfvars files that are safe to share if necessary
# !example.tfvars

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore lock files
.terraform.lock.hcl

# pki
# Ignore any .pem files (private keys)
*.pem

# Ignore any files that might contain credentials
*credentials*
*account.json
*secret*

# Ignore .env files which might contain environment variables with secrets
.env

# Ignore local backups
*.backup
*~

# Ignore log files
*.log


# IDE
# Ignore IDE and editor folders
.idea/
.vscode/
*.swp
*.swo